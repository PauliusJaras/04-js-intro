function hexHash(code){
    let arr = []
    for(let i = 0; i < code.length; i++){
        console.log(code[i].charCodeAt(0).toString(16));
        let numSum = 
        arr.push[]
    }

  }

  hexHash("Yo");